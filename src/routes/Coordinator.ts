export const goToHomePage = (history: string[]) => {
    history.push("/");
  };
  
  export const goToGamePage = (history: string[]) => {
    history.push("/games");
  };
  
  export const goToScorePage = (history: string[]) => {
    history.push("/score")
  };